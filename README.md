# URLShortener
A URL Shortener web application built using Node.js, Express.js, MongoDB Atlas, Mongoose, and Postman. The application generates unique short URLs, stores URL mappings in MongoDB, and redirects users to the original URLs using short codes.
